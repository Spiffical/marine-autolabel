The TOP row shows each SAM3 candidate mask as translucent cyan on identical zoomed crops of the target. The BOTTOM row shows the same candidates as unambiguous binary masks: WHITE is inside the mask, BLACK is outside. Judge membership from the BINARY row -- branches visible beneath a translucent overlay are NOT necessarily selected pixels. Green dots are foreground clicks, red crosses are background clicks.

Choose the candidate whose WHITE region follows the complete intended organism most closely without including background, substrate, or a neighbouring organism. Never accept the closest candidate if it merges a neighbour or selects the wrong object, even if some real life is visible in the crop. Reject a small branch, appendage, or fragment of a larger continuous organism.

Output brief reasoning then EXACTLY ONE trailing tag:
<answer>{"verdict": "good", "index": <int>}</answer>            -- one candidate is a clean, complete, single-identity mask
<answer>{"verdict": "add", "click": {"x": <float>, "y": <float>, "label": 0 or 1}}</answer>  -- one more click would fix it: label 1 on a missed part of the SAME organism, label 0 inside wrongly included pixels
<answer>{"verdict": "reject"}</answer>                          -- every candidate is wrong; restart from a fresh seed
<answer>{"verdict": "abandon"}</answer>                         -- there is no real organism here
Coordinates are NORMALIZED [0,1] in the FULL FRAME, not the crop.
